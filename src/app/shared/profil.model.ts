export class Profil {
    // id: number;
    // libelle: string;

    constructor(private id: number, private libelle: string) {
        // this.id = idProfil;
        // this.libelle = libelleProfil;
    }
}
